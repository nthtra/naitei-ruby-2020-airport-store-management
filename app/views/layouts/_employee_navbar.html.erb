<nav class="main-header navbar navbar-expand navbar-white navbar-light">
  <ul class="navbar-nav">
    <li class="nav-item">
      <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
    </li>
    <li class="nav-item d-none d-sm-inline-block">
      <%= link_to t(".home"), root_path, class: "nav-link" %>
    </li>
    <li class="nav-item d-none d-sm-inline-block">
      <a href="#" class="nav-link"><%= t ".contact" %></a>
    </li>
  </ul>
  <ul class="navbar-nav ml-auto">
    <li class="nav-item dropdown">
      <%= link_to "#", class: "nav-link", data: {toggle: "dropdown"} do%>
        <% if I18n.locale == :en %>
          <i class="flag-icon flag-icon-us"></i>
        <% else %>
          <i class="flag-icon flag-icon-vn"></i>
        <% end %>
      <% end %>
      <div class="dropdown-menu dropdown-menu-right p-0">
        <%= link_to t(".english"), {locale: :en}, class: "dropdown-item" %>
        <%= link_to t(".vietnamese"), {locale: :vi}, class: "dropdown-item" %>
      </div>
    </li>
    <li class="nav-item dropdown">
      <div class="nav-link" data-toggle="dropdown" id="notification_button">
        <i class="far fa-bell"></i>
        <% if unread_noti_quantity("Employee") > 0 %>
          <span class="badge badge-warning navbar-badge" id=<%= "notification_manager_#{current_employee.id}" %>><%= unread_noti_quantity("Employee") %></span>
        <% end %>
      </div>
      <div class="d-none" id="notification_url">
        <%= notifications_path %>
      </div>
      <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right" id="notification_dropdown">
      </div>
    </li>
    <li class="nav-item dropdown">
      <a class="nav-link" data-toggle="dropdown" href="#">
        <i class="fas fa-ellipsis-v"></i>
      </a>
      <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
        <% if employee_signed_in? %>
          <div class="dropdown-divider"></div>
          <%= link_to current_employee, class: "dropdown-item dropdown-footer" do %>
            <i class="fas fa-user"></i>
            <%= t ".profile" %>
          <% end %>
          <div class="dropdown-divider"></div>
          <%= link_to destroy_employee_session_path, method: :delete, class: "dropdown-item dropdown-footer" do %>
            <i class="fas fa-sign-out-alt"></i>
            <%= t ".logout" %>
          <% end %>
        <% end %>
      </div>
    </li>
  </ul>
</nav>
